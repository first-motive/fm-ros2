# fm_policy_train

Model training. May move to cloud — kept split-ready for that reason.
**Placeholder stub** — the `trainer` node only logs that it is up; no training
logic, topics, or parameters yet.

## Run

```bash
ros2 run fm_policy_train trainer
```

## Build Type

`ament_python`.
