# fm_data_record

Record episodes to LeRobot format. Entry point of the data engine.
**Placeholder stub** — the `recorder` node only logs that it is up; no capture
logic, topics, or parameters yet.

## Run

```bash
ros2 run fm_data_record recorder
```

## Build Type

`ament_python`.
