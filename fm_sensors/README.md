# fm_sensors

Multi-sensor capture layer. **Placeholder stub** — marks the slot where external
sensors (cameras, depth, IMUs, contact) feed the capture pipeline. No real
capture logic yet; the node only logs that it is up.

## Run

```bash
ros2 run fm_sensors sensor_node
```

## Build type

`ament_python`.
